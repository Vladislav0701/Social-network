let initialState = {
    friends: [
        { name: "Andrew", id: 1},
        { name: "Sveta", id: 2},
        { name: "Nastay", id: 3}
      ]
}

const sitebarReducer = (state = initialState, action) => {
    return state
}

export default sitebarReducer;